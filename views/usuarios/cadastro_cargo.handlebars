<h1>Escolha seu cargo e comfirme: </h1>

<form action="/usuarios/cadastro/cargo" method="post">
    <input type="hidden" name="id" value="{{usuario.dataValues.id}}">
    <select name="cargo">
        {{#each cargos}}
            <option value="{{dataValues.id}}">{{dataValues.cargo}}</option>
        {{else}}
            <option value="0">Nenhum cargo registrado</option>
        {{/each}}
    </select><br><br>
    <label>Confirme se você realmente quer se cadastrar com esse cargo:</label>
    <input type="radio" required><br><br>
    <button type="submit">Cadastrar cargo</button>
</form>